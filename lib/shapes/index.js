const { createCircle } = require('./circle')
const { createCylinder } = require('./cylinder')
const { createEllipse } = require('./ellipse')
const { createSphere } = require('./sphere')

module.exports = {
  createCircle,
  createCylinder,
  createEllipse,
  createSphere,
}
